Exist
  basicCheck ->
    solipsism = true if mind? and not world?

    speed = 0
    speed ?= 15

    footprints = yeti ? "bear"

  accessorVariant ->
    zip = lottery.drawWinner()?.address?.zipcode
